#
# Trigger a "no such method" runtime error for a bytecode method.
#

$modname = FailNoBytecodeMethod;

method start<> void {

$limstack = 0;
$limlocal = 0;

    call no_such_bytecode_method;
    leave;
    
}
