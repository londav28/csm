#
# Trigger a "no such method" runtime error for a native method.
#

$modname = FailNoNativeMethod;

method start<> void {

$limstack = 0;
$limlocal = 0;

    call csm:no_such_native_method;
    leave;
    
}
